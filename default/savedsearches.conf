#   Version 9.2.0
[Errors in the last 24 hours]
search = error OR failed OR severe OR ( sourcetype=access_* ( 404 OR 500 OR 503 ) )
dispatch.earliest_time = -1d

[Errors in the last hour]
search = error OR failed OR severe OR ( sourcetype=access_* ( 404 OR 500 OR 503 ) )
dispatch.earliest_time = -1h

[Messages by minute last 3 hours]
search = index=_internal source="*metrics.log" eps "group=per_source_thruput" NOT filetracker | eval events=eps*kb/kbps | timechart fixedrange=t span=1m limit=5 sum(events) by series
dispatch.earliest_time = -3h
displayview = report_builder_display

[Splunk errors last 24 hours]
search = index=_internal " error " NOT debug source=*splunkd.log*
dispatch.earliest_time = -24h

[Orphaned scheduled searches]
search = | rest timeout=600 splunk_server=local /servicesNS/-/-/saved/searches add_orphan_field=yes count=0 \
| search orphan=1 disabled=0 is_scheduled=1 \
| eval status = if(disabled = 0, "enabled", "disabled") \
| fields title eai:acl.owner eai:acl.app eai:acl.sharing orphan status is_scheduled cron_schedule next_scheduled_time next_scheduled_time actions \
| rename title AS "search name" eai:acl.owner AS owner eai:acl.app AS app eai:acl.sharing AS sharing

# For license usage report dashboard
[License Usage Data Cube]
dispatch.earliest_time = -31d
dispatch.latest_time = -0d
auto_summarize = 0
auto_summarize.dispatch.earliest_time = -1mon@d
auto_summarize.cron_schedule = 3,13,23,33,43,53 * * * *
search = index=_internal source=*license_usage.log* type="Usage" | eval h=if(len(h)=0 OR isnull(h),"(SQUASHED)",h) | eval s=if(len(s)=0 OR isnull(s),"(SQUASHED)",s) | eval idx=if(len(idx)=0 OR isnull(idx),"(UNKNOWN)",idx) | bin _time span=1d | stats sum(b) as b by _time, pool, s, st, h, idx

# For bucket merger
[Bucket Merge Retrieve Conf Settings]
search = | rest services/data/indexes datatype=all \
| search disabled!=1 AND bucketMerging=1 \
| dedup title \
| fields title bucketMerging bucketMerge.minMergeSizeMB bucketMerge.maxMergeSizeMB bucketMerge.maxMergeTimeSpanSecs bucketMerge.minMergeCount bucketMerge.maxMergeCount

[Login_Failure_Alert_Supression]
action.email = 1
action.email.message.alert = The alert condition for '$name$' was triggered. Multiple Login Failures.\
\
Login Failed for more than 5times
action.email.sendpdf = 1
action.email.sendresults = 1
action.email.to = varsha.mohandass@softmania.in
action.email.useNSSubject = 1
action.webhook.enable_allowlist = 0
alert.suppress = 0
alert.track = 0
counttype = number of events
cron_schedule = 5 * * * *
disabled = 1
dispatch.earliest_time = 0
display.general.type = statistics
display.page.search.mode = verbose
display.page.search.tab = statistics
enableSched = 1
quantity = 0
relation = greater than
request.ui_dispatch_app = search
request.ui_dispatch_view = search
search = index=botsv* sourcetype=wineventlog EventCode=4625 \
| stats count by Account_Name, dest \
| where count >= 10 \
| lookup failed_login_history Account_Name OUTPUT alert_count \
| eval alert_count=if(isnull(alert_count), 0, alert_count) \
| eval alert_count = alert_count + 1 \
| eval alert_count=if(alert_count=6,1,alert_count) \
| outputlookup failed_login_history.csv \
| where alert_count=1 \
```| eval team_email=case(\
    account_type=="Admin", "varshamanasa9903@gmail.com",\
    account_type=="Regular User", "varsha.mohandass@softmania.in"\
    ) \
| eval email_body=case(\
    account_type=="Admin", "🚨 Urgent: Multiple failed logins detected for Admin account: ".Account_Name.". Please investigate immediately!",\
    account_type=="Regular User", "⚠️ Multiple failed login attempts detected for user ".Account_Name.". Please check if assistance is needed."\
    ) \
| map search="search| sendemail to=$team_email$ subject=\"Security Alert: Malware Detected on $dest$\" message=\"$email_body$\" " ```

[Login_Failure_Alert_Suppression]
action.email = 1
action.email.message.alert = The alert condition for '$name$' was triggered.\
\
Login Failure Alert\
\
Skipped last 5alerts
action.email.sendpdf = 1
action.email.sendresults = 1
action.email.to = varsha.mohandass@softmania.in
action.email.useNSSubject = 1
action.webhook.enable_allowlist = 0
alert.suppress = 0
alert.track = 0
counttype = number of events
cron_schedule = */15 * * * *
disabled = 1
dispatch.earliest_time = 0
display.general.type = statistics
display.page.search.tab = statistics
enableSched = 1
quantity = 0
relation = greater than
request.ui_dispatch_app = search
request.ui_dispatch_view = search
search = index=botsv* sourcetype=wineventlog EventCode=4625 Account_Name!="-"\
| stats count by Account_Name, dest \
| where count >= 10 \
| lookup failed_login_history Account_Name OUTPUT alert_count \
| eval alert_count=if(isnull(alert_count), 0, alert_count) \
| eval alert_count = alert_count + 1 \
| eval alert_count=if(alert_count=6,1,alert_count) \
| outputlookup failed_login_history.csv \
| where alert_count=1

[Login Failures OR New Service Installation Alert]
action.email = 1
action.email.to = varsha.mohandass@softmania.in
action.email.useNSSubject = 1
action.webhook.enable_allowlist = 0
alert.suppress = 0
alert.track = 0
counttype = number of events
cron_schedule = 57 * * * *
disabled = 1
dispatch.earliest_time = 0
display.general.type = statistics
display.page.search.tab = statistics
enableSched = 1
quantity = 0
relation = greater than
request.ui_dispatch_app = search
request.ui_dispatch_view = search
search = index=botsv* sourcetype=wineventlog EventCode=4625 OR EventCode=4697\
|table Account_Name,EventCode, dest\
| stats count by Account_Name,EventCode, dest\
| where count > 3\
| eval team_email=case(\
    EventCode=4625, "varshamanasa9903@gmail.com",\
    EventCode=4697, "varsha.mohandass@softmania.in"\
)\
| eval email_body=case(\
    EventCode=4625, "🚨 Urgent: Multiple failed logins detected for Admin account: ".Account_Name.". Please investigate immediately!",\
    EventCode=4697, "⚠️ Multiple  Services has been installed by user ".Account_Name.". Please check if assistance is needed."\
)\
| map search="search| sendemail to=$team_email$ subject=\"Security Alert: Malware Detected on $dest$\" message=\"$email_body$\" "

[Login Failure]
action.email = 1
action.email.message.alert = The alert condition for '$name$' was triggered.\
\
$result.email_body$
action.email.to = $result.team_email$
action.email.useNSSubject = 1
action.webhook.enable_allowlist = 0
alert.suppress = 1
alert.suppress.period = 5h
alert.track = 0
counttype = number of events
cron_schedule = 38 * * * *
disabled = 1
dispatch.earliest_time = 0
display.general.type = statistics
display.page.search.tab = statistics
enableSched = 1
quantity = 0
relation = greater than
request.ui_dispatch_app = search
request.ui_dispatch_view = search
search = index=botsv* sourcetype=wineventlog EventCode=4625\
| eval account_type=if(match(lower(Account_Name),"administrator"), "Admin", "Regular User")\
| stats count by Account_Name, account_type, dest\
| where count > 50\
| eval team_email=case(\
    account_type=="Admin", "kaliyappan.rangan@softmania.in",\
    account_type=="Regular User", "varsha.mohandass@softmania.in"\
)\
| eval email_body=case(\
    account_type=="Admin", "🚨 Urgent: Multiple failed logins detected for Admin account: ".Account_Name.". Please investigate immediately!",\
    account_type=="Regular User", "⚠️ Multiple failed login attempts detected for user ".Account_Name.". Please check if assistance is needed."\
)

[ServiceNow_Incident_Creation]
action.email.inline = 1
action.snow_incident = 1
action.snow_incident.param.account = Softmania
action.snow_incident.param.assignment_group = "Security"
action.snow_incident.param.category = "Security"
action.snow_incident.param.impact = 2
action.snow_incident.param.priority = 4
action.snow_incident.param.scripted_endpoint = /api/now/table/incident
action.snow_incident.param.short_description = "Multiple failed login attempts detected from "$result.Account_Name$" Account to "$result.dest$" destination"
action.snow_incident.param.splunk_url = http://13.235.187.5:8000
action.snow_incident.param.state = 1
action.snow_incident.param.subcategory = "Cyber Attack"
action.snow_incident.param.urgency = 2
action.webhook.enable_allowlist = 0
alert.digest_mode = 0
alert.suppress = 0
alert.track = 0
counttype = number of events
cron_schedule = 0,15,30,45 * * * *
dispatch.earliest_time = 0
display.general.type = statistics
display.page.search.mode = verbose
display.page.search.tab = statistics
enableSched = 1
quantity = 0
relation = greater than
request.ui_dispatch_app = search
request.ui_dispatch_view = search
search = index=botsv* sourcetype=wineventlog EventCode=4625 Account_Name!="-"\
| stats count by Account_Name, dest\
| where count >= 3\
| eval incident_description = "Multiple failed login attempts detected from " + Account_Name + " Account to " + dest + " destination"\
| lookup servicenow_incident_history description AS incident_description OUTPUT status\
| eval status=if(isnull(status), "Latest", status) \
| where status="Resolved" OR status="Closed" OR status="Latest"

[servicenow_incident_history]
action.lookup = 1
action.lookup.filename = servicenow_incident_history.csv
action.webhook.enable_allowlist = 0
alert.suppress = 0
alert.track = 0
counttype = number of events
cron_schedule = 10,25,40,55 * * * *
dispatch.earliest_time = 0
display.general.type = statistics
display.page.search.tab = statistics
enableSched = 1
quantity = 0
relation = greater than
request.ui_dispatch_app = search
request.ui_dispatch_view = search
search = index=main sourcetype="snow:incident" | table description, status | stats latest(status) as status by description

[Login_Failure_Report]
action.email.useNSSubject = 1
action.webhook.enable_allowlist = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = 0
cron_schedule = */3 * * * *
dispatch.earliest_time = 0
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
enableSched = 1
request.ui_dispatch_app = search
request.ui_dispatch_view = search
search = index=botsv3 sourcetype=wineventlog*\
| stats count by src_ip\
| sort - count

[Wineventlog_Report]
action.email.useNSSubject = 1
action.webhook.enable_allowlist = 0
alert.track = 0
auto_summarize = 1
auto_summarize.dispatch.earliest_time = 0
cron_schedule = */2 * * * *
dispatch.earliest_time = 0
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
enableSched = 1
request.ui_dispatch_app = search
request.ui_dispatch_view = search
search = index=botsv3 sourcetype=wineventlog*\
| stats count by src_ip\
| sort - count

